---
'@oclkadev/node-starter': minor
---

Add CLI scaffold with Commander: command structure (`commands/`), shared options, centralized I/O layer (`io` with quiet/verbose/dryRun), typed error handling (`AppError`, `ErrorCode`, `formatErrorMessage`, `getErrorMessage`), string helpers, and entry point with formatted error output.
